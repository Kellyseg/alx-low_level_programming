#include "main.h"

/**
 * mul - multiplies two integers
 * @a: character from ASCII code
 * @c: chaaracter from ASCII code
 *
 * Return: always 0(success)
 */
int mul(int a, int c)
{
	return (a * c);
}
