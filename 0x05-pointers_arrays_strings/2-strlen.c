#include "main.h"
#include <string.h>

/**
 * _strlen - function that returns lenght of a string
 *@s: string
 * Return: length of string
 */
int _strlen(char *s)
{
	int l;

	l = strlen(s);
	return (l);
}
