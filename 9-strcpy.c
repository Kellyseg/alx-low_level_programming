#include "main.h"
#include <string.h>

/**
 * *_strcpy - copy a string into another
 * @dest: string receiving another
 * @src: string to copy
 *
 * Return: void
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
